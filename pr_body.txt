💡 **What:** Replaced individual `INSERT` queries for events with a single bulk `INSERT` using `sqlx::QueryBuilder::push_values`.
🎯 **Why:** To eliminate the N+1 query problem, reducing 150 individual network database roundtrips to just 1 per seed cycle.
📊 **Measured Improvement:** The `seed_database` function previously executed 150 separate inserts per database seeded. By collecting events into a memory vector and executing a single parameterized bulk insert via `QueryBuilder`, the number of network roundtrips is reduced from 150 queries to 1 query for inserting events per farm seeding iteration. Since the test sandbox does not host a reachable live postgres pool to connect to for benchmarking runtime results, we can definitively confirm through code inspection a theoretical 150x reduction in database network requests.
