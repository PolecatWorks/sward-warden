apiVersion: v1
kind: Service
metadata:
  name: {{ include "sward-warden.fullname" . }}-be
  labels:
    {{- include "sward-warden.be.labels" . | nindent 4 }}
spec:
  type: {{ .Values.be.service.type }}
  ports:
    - name: http-web
      port: {{ .Values.be.service.port }}
      targetPort: http-web
      protocol: TCP
    - name: http-envoy-prom
      port: 15020
      protocol: TCP
      targetPort: 15020
  selector:
    {{- include "sward-warden.be.selectorLabels" . | nindent 4 }}
