apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "sward-warden.serviceAccountName" . }}
  labels:
    {{- include "sward-warden.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- with .Values.imagePullSecrets }}
imagePullSecrets:
  {{- toYaml . | nindent 2 }}
{{- end }}
